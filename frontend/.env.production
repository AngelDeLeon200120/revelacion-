# URL del backend en producción.
# Reemplazá "tudominio.com" por tu dominio real (el subdominio del backend en Hostinger).
#
# En Vercel esto se puede pisar con la variable de entorno VITE_API_URL
# (Project Settings -> Environment Variables). La de Vercel tiene prioridad.
VITE_API_URL=https://api.tudominio.com
